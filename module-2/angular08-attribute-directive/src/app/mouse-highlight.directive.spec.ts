import { MouseHighlightDirective } from './mouse-highlight.directive';

describe('MouseHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
