import { DepartmentListPipe } from './department-list.pipe';

describe('DepartmentListPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentListPipe();
    expect(pipe).toBeTruthy();
  });
});
